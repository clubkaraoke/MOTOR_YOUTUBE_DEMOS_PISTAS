name: Verify C1 OAuth

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Configure SSH
        env:
          OVH_HOST: ${{ secrets.OVH_HOST }}
          OVH_PORT: ${{ secrets.OVH_PORT }}
          OVH_SSH_KEY: ${{ secrets.OVH_SSH_KEY }}
        run: |
          set -euo pipefail
          install -m 700 -d "$HOME/.ssh"
          printf '%s\n' "$OVH_SSH_KEY" | tr -d '\r' > "$HOME/.ssh/id_ed25519"
          chmod 600 "$HOME/.ssh/id_ed25519"
          {
            printf '%s ssh-ed25519 %s\n' "$OVH_HOST" 'AAAAC3NzaC1lZDI1NTE5AAAAICvS+PnyRvMbs7D9w+ZRAhtbGtMnlXvZG1eEp+APNR2V'
            printf '[%s]:%s ssh-ed25519 %s\n' "$OVH_HOST" "$OVH_PORT" 'AAAAC3NzaC1lZDI1NTE5AAAAICvS+PnyRvMbs7D9w+ZRAhtbGtMnlXvZG1eEp+APNR2V'
          } > "$HOME/.ssh/known_hosts"
      - name: Verify C1 against YouTube
        env:
          OVH_HOST: ${{ secrets.OVH_HOST }}
          OVH_PORT: ${{ secrets.OVH_PORT }}
          OVH_USER: ${{ secrets.OVH_USER }}
        run: |
          set -euo pipefail
          ssh -p "$OVH_PORT" "$OVH_USER@$OVH_HOST" 'bash -s' <<'REMOTE'
          set -euo pipefail
          cd /opt/djgabo-youtube
          sudo docker compose -f docker-compose.prod.yml exec -T web python - <<'PY'
          from app.core.database import SessionLocal
          from app.models.entities import Channel
          from app.services.youtube import _service
          with SessionLocal() as db:
              c = db.get(Channel, 1)
              print("DB_OAUTH_STATUS", c.display_name, c.oauth_status)
              try:
                  response = _service(c).channels().list(part="id,snippet", mine=True).execute()
                  items = response.get("items", [])
                  if not items:
                      print("C1_API", "ERROR", "SIN_CANAL")
                  else:
                      print("C1_API", "OK", items[0].get("snippet", {}).get("title"))
              except Exception as exc:
                  msg = str(exc)
                  reason = "INVALID_GRANT" if "invalid_grant" in msg else ("EXPIRED_OR_REVOKED" if "expired or revoked" in msg else type(exc).__name__)
                  print("C1_API", "ERROR", reason)
          PY
          REMOTE
