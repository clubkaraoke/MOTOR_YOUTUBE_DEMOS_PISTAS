name: Inspect Opening Ending smoke failure 2

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/inspect-opening-ending-smoke-failure-2.yml"

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - uses: actions/checkout@v4
      - name: Configure SSH
        env:
          OVH_HOST: ${{ secrets.OVH_HOST }}
          OVH_PORT: ${{ secrets.OVH_PORT }}
          OVH_SSH_KEY: ${{ secrets.OVH_SSH_KEY }}
        run: |
          install -m 700 -d "$HOME/.ssh"
          printf '%s\n' "$OVH_SSH_KEY" | tr -d '\r' > "$HOME/.ssh/id_ed25519"
          chmod 600 "$HOME/.ssh/id_ed25519"
          ssh-keyscan -p "$OVH_PORT" -t ed25519 "$OVH_HOST" 2>/dev/null > "$HOME/.ssh/known_hosts"
      - name: Read synthetic files
        env:
          OVH_HOST: ${{ secrets.OVH_HOST }}
          OVH_PORT: ${{ secrets.OVH_PORT }}
          OVH_USER: ${{ secrets.OVH_USER }}
        run: |
          ssh -p "$OVH_PORT" "$OVH_USER@$OVH_HOST" 'bash -s' <<'REMOTE'
          TEST=/tmp/djgabo-opening-ending-smoke
          echo "FILES:"
          find "$TEST" -maxdepth 2 -type f -printf '%p %s\n' 2>/dev/null | sort || true
          for f in "$TEST/off.log" "$TEST/on.log"; do
            if [ -f "$f" ]; then echo "=== $f ==="; cat "$f"; fi
          done
          REMOTE
